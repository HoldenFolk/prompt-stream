// Central place for tunables / defaults (reusable across pages)
export const DEFAULT_PARAMS = {
    // You can add temperature/topK when supported by your Chrome version
    // temperature: 1,
    // topK: 3,
  };
  
  export const UI_STRINGS = {
    unavailable:
      "Model unavailable on this device. Check Chrome version and hardware/storage requirements.",
    initFirst: "Please click “Init / Download model” first.",
  };
  